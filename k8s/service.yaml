apiVersion: v1
kind: Service
metadata:
  name: rednote-mcp-service
  labels:
    app: rednote-mcp
spec:
  selector:
    app: rednote-mcp
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP  # Use LoadBalancer or NodePort if external access is needed